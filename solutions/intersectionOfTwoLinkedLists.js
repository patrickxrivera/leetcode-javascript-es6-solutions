// Definition for singly-linked list.
function ListNode(val) {
  this.val = val;
  this.next = null;
}

/**
 * @param {ListNode} headA
 * @param {ListNode} headB
 * @return {ListNode}
 */

const getIntersectionNode = (headA, headB) => {
  // get length of a
  // get length of b
  // get difference
  // start longer list at length a - length b
  // while ptrA and ptrB are not null
  // if lists are equal
  // return currPtr
  // else
  // increment node
  // return null
};
